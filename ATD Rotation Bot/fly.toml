app = "atd-rotation-bot"
primary_region = "ewr"

[build]

[env]
  STATE_DIR = "/data"

[[mounts]]
  source      = "atd_rotation_bot_data"
  destination = "/data"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

app = 'rapm-bot'
primary_region = 'lhr'
kill_signal = 'SIGINT'
kill_timeout = '30s'

[build]
  dockerfile = 'Dockerfile'

[build.args]
  BUILDKIT_INLINE_CACHE = "1"

[[mounts]]
  source = 'rapm_cache'
  destination = '/cache'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '1024'
